{
  "name": "bun-htmx-crud",
  "module": "src/index.ts",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "update:types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "create:db": "wrangler d1 create bun-htmx-crud",
    "migrate:create": "drizzle-kit generate",
    "migrate:dev": "wrangler d1 migrations apply bun-htmx-crud --local",
    "migrate": "wrangler d1 migrations apply bun-htmx-crud --remote",
    "test:db:dev": "wrangler d1 execute bun-htmx-crud --local --command \"select * from products\";",
    "test:db": "wrangler d1 execute bun-htmx-crud --remote --command \"select * from products\";",
    "dev": "wrangler dev",
    "deploy:app": "wrangler deploy",
    "secret:github": "wrangler secret put GITHUB_CLIENT_SECRET",
    "secret:google": "wrangler secret put GOOGLE_CLIENT_SECRET",
    "logtail": "wrangler tail bun-htmx-crud",
    "rollback:app": "wrangler rollback",
    "delete:app": "wrangler delete",
    "databases": "wrangler d1 list",
    "studio:db": "drizzle-kit studio",
    "studio:db:dev": "cross-env LOCAL_DB_PATH=$(find .wrangler/state/v3/d1/miniflare-D1DatabaseObject -type f -name '*.sqlite' -print0 | xargs -0 ls -t | head -1) drizzle-kit studio --config=drizzle.config.local.ts",
    "introspect:db": "drizzle-kit introspect",
    "dbcat:db:dev": "cross-env find .wrangler/state/v3/d1/miniflare-D1DatabaseObject -type f -name '*.sqlite' -print0 | xargs -0 ls -t | head -1 | xargs bunx dbcat"
  },
  "dependencies": {
    "@elysiajs/html": "^1.4.0",
    "drizzle-orm": "^0.45.1",
    "elysia": "^1.4.21",
    "typedi": "^0.10.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.5",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.3",
    "drizzle-kit": "^0.31.8",
    "typed-html": "^3.0.1",
    "wrangler": "^4.57.0"
  }
}
